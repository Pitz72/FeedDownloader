# Changelog v1.3.12

**Data:** 23 giugno 2026

Due correzioni richieste dall'uso reale: il download non recuperava tutti gli episodi
dei feed grandi e le notifiche di nuovi episodi non erano affidabili.

## Download — Supporto ai feed paginati (RFC 5005)

`FeedService.parseFeed()` scaricava e analizzava **solo la prima pagina** del feed. Per i
provider che paginano il feed RSS (Apple Podcasts, Libsyn, Blubrry…) erano visibili solo
gli ultimi 20–100 episodi; il resto del catalogo era invisibile e quindi non scaricabile.

* **Fix:** dopo la prima pagina, il parser segue i link `<atom:link rel="next" href="…">`
  (e `<link rel="next">`), scaricando e unendo gli episodi di tutte le pagine in un unico
  elenco. Limite di sicurezza di **20 pagine** contro loop infiniti.
* La logica di fetch è stata estratta in `fetchPage()` così ogni pagina (prima e
  successive) passa attraverso gli **stessi controlli di sicurezza** (SSRF, blocco DOCTYPE
  XXE, `maxContentLength` 15 MB).

## Notifiche — Rilevamento nuovi episodi basato sui GUID

Il refresh in background confrontava il **conteggio numerico** degli episodi
(`newCount > previousCount`). Con i podcast a finestra mobile (pubblicano nuovi episodi
rimuovendone di vecchi, prassi comune) il conteggio non cresceva → **nessuna notifica**.
Era la causa per cui le notifiche "sembravano non funzionare".

* **Fix:** nuova tabella `known_episodes` (GUID per feed). Il rilevamento ora confronta i
  GUID effettivi: un episodio è "nuovo" se il suo GUID non era ancora noto, a prescindere
  dal conteggio totale.
* **Baseline silenziosa:** alla prima analisi di un feed (`PARSE_FEED`) i GUID vengono
  registrati come noti, e se il refresh in background trova un feed senza baseline
  (installazioni aggiornate da una versione precedente, o feed importati via OPML e mai
  aperti) lo seeda **senza notificare** — evita una valanga di false notifiche al primo
  controllo dopo l'aggiornamento.
* Pulizia di `known_episodes` alla rimozione di un feed (`REMOVE_FEED`).
* Grammatica italiana della notifica corretta (prima: *"3 nuovi episodii trovatii"*).

## UI — Badge sidebar "DA SCARICARE"

Il badge mostrava "N NUOVI" ma il valore è `episodeCount − scaricati` (episodi non ancora
scaricati, non "nuovi"). Rinominato in **"DA SCARICARE"** e localizzato correttamente in
tutte e 8 le lingue (le chiavi `new_count_one/other` mancavano dai file di traduzione, così
veniva usato il fallback italiano in ogni lingua).

## Sicurezza — Hardening della paginazione (SSRF / cicli)

Difese in profondità sul nuovo codice di paginazione (oltre al guard SSRF già attivo a
livello socket via `safeHttp`):

* I link `<atom:link rel="next">` relativi vengono **risolti** contro l'URL della pagina
  che li contiene (RFC 5005 li ammette relativi).
* Ogni URL di pagina successiva passa per `validateUrl` (whitelist http/https + blocco IP
  privati/riservati): un feed ostile non può "camminare" la paginazione verso host interni
  o schemi non-web.
* Rilevamento **cicli** (`visited` set): un feed che punta `next` a se stesso si ferma
  subito invece di consumare tutte le 20 pagine.

## Infrastruttura test — Suite riparata e resa gate bloccante

I ~66 test che usano `better-sqlite3` fallivano per **mismatch ABI** del modulo nativo:
l'app gira sotto Electron (NODE_MODULE_VERSION 123), vitest sotto Node (127), e un modulo
nativo ha un solo binario compilato alla volta.

* **Soluzione automatica:** aggiunto `@electron/rebuild` (devDep) + script npm che gestiscono
  l'ABI in modo invisibile:
  * `pretest` / `pretest:watch` / `pretest:coverage` → `rebuild:node` (binario per Node prima dei test)
  * `posttest` → `rebuild:electron` (ripristina il binario per Electron dopo i test)
  * `predev` → `rebuild:electron` (l'app parte sempre con l'ABI giusta)
  Il dev lancia `npm test` e `npm run dev` senza pensare all'ABI.
* Corretta un'asserzione obsoleta in `DatabaseService.test.ts` (header CSV esteso v0.7.4).
* CI (`build.yml`): lo step test passa da *informational* a **bloccante** (`npx vitest run`);
  un test rosso ora ferma la pubblicazione.

## Pubblicità (Spreaker/DAI) — non fattibile

Confermata la non-fattibilità della rimozione spot: lo stitching server-side fonde gli
annunci nel binario MP3 a ogni download. Nessuna modifica.

## Verifica

* `tsc --noEmit` (renderer) — clean. `tsc -p tsconfig.electron.json` (main) — clean.
  `eslint` — clean.
* Suite completa: **199/199 test passano** (11 file) sotto Node, ABI ripristinata per
  Electron automaticamente. `FeedService.test.ts` riallineato all'API corrente + 4 nuovi
  test (paginazione, SSRF guard, risoluzione URL relativi).

## File modificati

* `electron/services/FeedService.ts` — paginazione RFC 5005 (`fetchPage`,
  `extractNextPageUrl`, loop con `MAX_PAGES`).
* `electron/services/DatabaseService.ts` — tabella + metodi `known_episodes`
  (`getKnownGuids`, `markGuidsAsKnown`, `findNewGuids`, `removeKnownEpisodes`).
* `electron/services/LibraryService.ts` — proxy dei metodi `known_episodes`.
* `electron/ipc.ts` — rilevamento via GUID + baseline silenziosa in `runBackgroundRefresh`,
  seeding su `PARSE_FEED`, cleanup su `REMOVE_FEED`, grammatica notifica IT.
* `electron/services/FeedService.ts` — hardening paginazione (validateUrl + risoluzione
  URL relativi + guard cicli).
* `src/components/Sidebar.tsx` — badge "DA SCARICARE".
* `src/locales/*.json` — chiavi `new_count_one/other` (8 lingue).
* `tests/FeedService.test.ts` — riallineato + test paginazione/SSRF (9 test).
* `tests/DatabaseService.test.ts` — asserzione header CSV aggiornata.
* `.github/workflows/build.yml` — test come gate bloccante.
* `package.json` — 1.3.11 → 1.3.12; badge README; `@electron/rebuild` + script ABI.
